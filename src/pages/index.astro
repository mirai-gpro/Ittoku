---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Hero from '../components/Hero.astro';
import VideoReel from '../components/VideoReel.astro';
import Story from '../components/Story.astro';
import Gallery from '../components/Gallery.astro';
import Plan from '../components/Plan.astro';
import PlanPhotos from '../components/PlanPhotos.astro';
import SNS from '../components/SNS.astro';
import Booking from '../components/Booking.astro';
import Location from '../components/Location.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="銀座一徳 Ginza Ittoku"
  description="A private cultural lounge in Ginza, Tokyo. ¥300M imperial interior. ¥33,000/person. Open 20:00–24:00."
>
  <Navbar />
  <Hero />
  <VideoReel />
  <Story />
  <Gallery />
  <Plan />
  <PlanPhotos />
  <SNS />
  <Booking />
  <Location />
  <Footer />
</Layout>

<script>
  // Language
  const gmapBase = 'https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d1620.7!2d139.7591649!3d35.6690261!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x60188b006feea3ab%3A0x860e8305c9473f15!2sGinza%20Ittoku!5e0!3m2!1s';

  function setLang(l: string) {
    document.body.setAttribute('data-lang', l);
    document.documentElement.lang = l === 'ja' ? 'ja' : 'en';
    document.getElementById('bEn')?.classList.toggle('on', l === 'en');
    document.getElementById('bJa')?.classList.toggle('on', l === 'ja');

    // Switch Google Map language
    const gmap = document.getElementById('gmap') as HTMLIFrameElement | null;
    if (gmap) {
      const lang = l === 'ja' ? 'ja' : 'en';
      gmap.src = gmapBase + lang + '!2sjp!4v1';
    }
  }

  // Language toggle buttons
  document.querySelectorAll('[data-lang-switch]').forEach(btn => {
    btn.addEventListener('click', () => {
      const lang = (btn as HTMLElement).getAttribute('data-lang-switch') || 'en';
      setLang(lang);
    });
  });

  // Scrolled nav
  const navEl = document.getElementById('nav');
  window.addEventListener('scroll', () => navEl?.classList.toggle('on', scrollY > 60), { passive: true });

  // Fade on scroll (IntersectionObserver)
  const io = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
  }, { threshold: 0.1 });
  document.querySelectorAll('.fade').forEach(el => io.observe(el));
</script>
