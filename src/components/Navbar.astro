<nav id="nav">
  <a href="#" class="logo"><img src="/images/logo_it.png" alt="一徳" class="logo-img"><small>Ginza Ittoku</small></a>
  <div class="nav-r">
    <!-- lang-sw hidden temporarily: English only mode -->
    <div class="lang-sw" style="display:none;">
      <button class="lb on" id="bEn" data-lang-switch="en">EN</button>
      <button class="lb" id="bJa" data-lang-switch="ja">日本語</button>
    </div>
    <a href="https://www.tablecheck.com/shops/ginza-ittoku/reserve" target="_blank" rel="noopener" class="nav-book en">Reserve</a>
    <a href="https://www.tablecheck.com/shops/ginza-ittoku/reserve" target="_blank" rel="noopener" class="nav-book ja">予約する</a>
  </div>
</nav>

<style>
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 1.25rem 2rem;
    background: linear-gradient(to bottom, rgba(0,0,0,.85), transparent);
    transition: background var(--ease), padding var(--ease);
  }
  nav.on { background: rgba(8,8,8,.97); padding: .8rem 2rem; backdrop-filter: blur(8px); border-bottom: 1px solid rgba(201,168,76,.12); }
  .logo { font-family: var(--fs); color: var(--gold); font-size: 1.7rem; letter-spacing: .05em; display: flex; flex-direction: column; align-items: flex-start; }
  .logo-img { height: 2rem; width: auto; display: block; }
  .logo small { display: block; font-size: .5rem; color: rgba(201,168,76,.55); letter-spacing: .35em; font-family: var(--fn); font-weight: 300; text-transform: uppercase; margin-top: 2px; }
  .nav-r { display: flex; align-items: center; gap: 1rem; }
  .lang-sw { display: flex; border: 1px solid rgba(201,168,76,.35); }
  .lb { background: none; border: none; color: rgba(255,255,255,.4); cursor: pointer; padding: .28rem .65rem; font-size: .72rem; font-family: var(--fn); transition: all var(--ease); }
  .lb.on, .lb:hover { background: var(--gold); color: var(--black); }
  .nav-book { background: var(--gold); color: var(--black); padding: .5rem 1.3rem; font-size: .78rem; font-family: var(--fn); letter-spacing: .08em; text-transform: uppercase; transition: background var(--ease); }
  .nav-book:hover { background: var(--gold-l); }
  @media (max-width: 768px) {
    nav { padding: 1rem 1.25rem; }
  }
</style>
